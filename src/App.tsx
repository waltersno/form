import { AppWrapper } from 'components/AppWrapper';
import { Form } from 'components/Form';

const App = () => {
  return (
    <AppWrapper>
      <Form />
    </AppWrapper>
  );
};

export default App;
